spring:
  jpa:
    # 전송 쿼리 확인
    show-sql: true
    properties:
      hibernate:
        format_sql: true

    # 테이블 생성 후에 data.sql 실행
    defer-datasource-initialization: true

  datasource:
    url: jdbc:h2:mem:testdb
    username: sa

  h2:
    console:
      enabled: true


# JWT 설정
jwt:
  secret-key: "mynameishongseokjunhihello19991020qksrkqtmqslek" # JWT 서명에 사용할 비밀키 (실제 환경에서는 환경 변수로 설정 권장)
  expiration:
    access-token: 900000 # 액세스 토큰 유효기간 (15분, 밀리초 단위)
    refresh-token: 604800000 # 리프레시 토큰 유효기간 (7일, 밀리초 단위)